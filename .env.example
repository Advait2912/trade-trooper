# =====================================================
# Alpaca Market Data API (https://alpaca.markets/)
# =====================================================
ALPACA_API_KEY=
ALPACA_API_SECRET=

# Optional: data feed. "iex" (free) or "sip" (paid). Defaults to iex.
ALPACA_DATA_FEED=iex

# Optional: options feed. "indicative" (free, delayed/modified) or "opra" (paid).
ALPACA_OPTIONS_FEED=indicative

# =====================================================
# Risk (Phase 3) — position sizing assumptions
# =====================================================
ACCOUNT_CAPITAL=100000
RISK_PER_TRADE_PCT=0.01
MAX_POSITION_PCT=0.05
MIN_RISK_REWARD=1.0

# =====================================================
# Decision (Phase 4) — trade gates
# =====================================================
# Minimum prediction confidence before the Decision Agent will trade.
MIN_CONFIDENCE=0.35

# =====================================================
# Trading (Phase 5) — execution (PAPER ONLY, kill-switched)
# =====================================================
# Set to true to allow the paper-trading loop (python main.py NVDA --trade).
TRADING_ENABLED=false
TRADING_INTERVAL_MIN=30
MAX_OPEN_POSITIONS=3
MAX_OPEN_POSITIONS_PER_TICKER=1
DAILY_LOSS_LIMIT_PCT=0.02
MAX_PORTFOLIO_DRAWDOWN_PCT=0.10
ORDER_TYPE=limit
LIMIT_SLIPPAGE_PCT=0.0
TRADE_HORIZON_DAYS=5

# =====================================================
# Ollama local inference
# =====================================================
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma4:e4b

# =====================================================
# Ollama web search (local experimental endpoint)
# Auth via OLLAMA_API_KEY (or `ollama signin`).
# =====================================================
OLLAMA_API_KEY=
OLLAMA_WEB_SEARCH_URL=http://localhost:11434/api/experimental/web_search
OLLAMA_WEB_FETCH_URL=http://localhost:11434/api/experimental/web_fetch
